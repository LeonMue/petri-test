struct ProductCatalog {
    string name;
    string description;
    list<Category> categories;
}

struct Category {
    string name;
    string description;
    list<Product> products;
}

struct Product {
    string name;
    string brand;
    int32 price;
    float64 weight;
    bool available;
    list<string> colors;
}